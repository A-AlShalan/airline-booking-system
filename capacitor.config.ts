import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Airline-booking-system',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
